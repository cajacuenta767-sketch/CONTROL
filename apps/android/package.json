{
  "name": "control-android",
  "version": "1.0.0",
  "private": true,
  "description": "CONTROL para Android: el panel de la agencia como app (Capacitor)",
  "scripts": {
    "preparar": "npx cap add android || true && npx cap sync android",
    "apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/preferences": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
